{
  "private": true,
  "name": "galileo-shorten",
  "version": "1.0.0",
  "description": "A link shortening system using CloudFlare workers and KV storage.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Hugh Stephens <hughstephens@hughstephens.com>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
